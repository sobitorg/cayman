
# pprint library is used to make the output look more pretty
from pprint import pprint

#update record
#AsingleAsset = db.assets.find_one({'Hostname': 'HAL2'})
#print('A single asset:')
#pprint(AsingleAsset)

#print(AsingleAsset.get('_id'))

#myquery = { "Hostname" : "HAL2" }
#newvalues = { "$set": { "Serial": "C02S51A5FVH8" } }

#db.assets.update_one(myquery, newvalues)
